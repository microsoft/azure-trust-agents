# Azure Trust Agents — Python 3.10+ required
#
# Microsoft Agent Framework 1.x. The former `agent-framework-azure-ai` package was
# removed upstream; Foundry clients now live in `agent-framework-foundry`.
# Targeted provider packages are used instead of the `agent-framework` meta package,
# which also pulls in unrelated providers (Bedrock, Gemini, Mistral, Ollama, ...).
agent-framework-core==1.14.0
agent-framework-foundry==1.11.0
agent-framework-devui==1.0.0b260813

# agent-framework-devui requires openai<3; do not bump to 3.x while DevUI is used.
openai>=2.45.0,<3

# Azure SDKs
azure-ai-projects>=2.3.0,<3
azure-identity>=1.25.0
azure-cosmos>=4.16.0

# Observability (Challenge 3)
# Exporter b45 is broken against opentelemetry-sdk 1.44 (LogData import); b56 pins sdk 1.43.
opentelemetry-api>=1.39.0,<2
opentelemetry-sdk>=1.39.0,<2
azure-monitor-opentelemetry-exporter==1.0.0b56

# Core runtime
pydantic>=2.11.0
python-dotenv>=1.1.0
typing-extensions>=4.14.0

# Notebook support (challenge-1/workflow/sequential_workflow.ipynb)
ipykernel>=6.29.0
